[project]
name = "clickup-daily-queue"
version = "0.1.0"
description = "MCP server exposing a daily ClickUp task queue for Claude Desktop"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]
